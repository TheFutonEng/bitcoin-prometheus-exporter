# Chain the test node runs on: regtest (default), signet, testnet4 or main.
# Change BITCOIN_COOKIE to match: bitcoind keeps mainnet's cookie at the data
# directory root and every other chain's under a subdirectory of the same name.
BITCOIN_CHAIN=regtest
BITCOIN_COOKIE=/data/regtest/.cookie
